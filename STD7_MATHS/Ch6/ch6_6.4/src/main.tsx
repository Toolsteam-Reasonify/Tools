import React from 'react'
import ReactDOM from 'react-dom/client'
import ExteriorAnglesTool from './ExteriorAnglesTool.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ExteriorAnglesTool />
  </React.StrictMode>,
)
